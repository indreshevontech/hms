jQuery(document).ready(function(a) {

    ThbImage = {
        uploader: function(b, c, d) {
            var e = wp.media({
                title: ThbImageWidget.frame_title,
                multiple: !1,
                library: {
                    type: "image"
                },
                button: {
                    text: ThbImageWidget.button_title
                }
            });
            return e.on("close", function() {
                var b = e.state().get("selection").toJSON();
                a("#" + c).val(b[0].url), a("#" + d).val(b[0].alt)
            }), e.open(), !1
        }
    }
});