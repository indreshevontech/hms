<?php if ( ! defined( 'FW' ) ) {
	die( 'Forbidden' );
}

$manifest = array();

$manifest['name']        = __( 'Social Twitter', 'fw' );
$manifest['description'] = __( 'Social Twitter', 'fw' );
$manifest['version']     = '1.0.0';
$manifest['display']     = 'social';
$manifest['standalone']  = true;