<?php if ( ! defined( 'FW' ) ) {
	die( 'Forbidden' );
}

$manifest = array();

$manifest['name']        = __( 'Social Facebook', 'fw' );
$manifest['description'] = __( 'Social Facebook', 'fw' );
$manifest['version']     = '1.0.0';
$manifest['display']     = 'social';
$manifest['standalone']  = true;